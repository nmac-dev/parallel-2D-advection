#include <stdio.h>

/*      Prototypes      */
extern int outputResultsFile(char *fileName, ) {
    FILE *outputFile;
    if( ( outputFile = fopen(fileName, 'w')) != NULL ) {

    } 
    else {
        fprint
    } 
} 